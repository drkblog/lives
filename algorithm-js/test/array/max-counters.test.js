import { maxCounters } from "../../src/array/max-counters";

test('case 1', () => {
  // const counters = 5;
  // const instructions = [3, 4, 4, 6, 1, 4, 4];
  // expect(maxCounters(instructions, counters)).toStrictEqual([3, 2, 2, 4, 2]);
});
